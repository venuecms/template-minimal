{
  "type": "object",
  "properties": {
    "theme": {
      "type": "string",
      "enum": ["larry", "darkGold", "default"],
      "default": "default"
    },
    "font": {
      "type": "string",
      "enum": ["jost", "IBMPlexMono"]
      "default": "jost"
    }
  }
}
